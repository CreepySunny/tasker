package tasks_test

import (
	"testing"
)

func TestTasks(t *testing.T) {
	// t.Run("Return task list", func(t *testing.T) {
	// 	want := []tasks.Task{
	// 		{ID: 1, Description: "Buy groceries", CreatedAt: time.Now(), IsCompleted: false},
	// 		{ID: 2, Description: "Clean the house", CreatedAt: time.Now(), IsCompleted: false},
	// 		{ID: 3, Description: "Finish homework", CreatedAt: time.Now(), IsCompleted: false},
	// 	}

	// 	got, err := tasks.ListTasks("")
	// 	if err != nil {
	// 		t.Errorf("Returned an error: %v", err)
	// 	}
	// 	if !reflect.DeepEqual(got, want) {
	// 		t.Errorf("got %v; want %v", got, want)
	// 	}
	// })
}
